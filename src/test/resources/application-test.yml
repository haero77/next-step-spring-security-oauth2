spring:
  config:
    activate:
      on-profile: test

security:
  oauth2:
    github:
      domain: http://localhost:8089
      client-id: "test-github-client-id"
      client-secret: "test-github-client-secret"
